import { createAction } from "@reduxjs/toolkit";

export const addCategory = createAction(
  "categories/addCategory"
    //   ,
    //   ({ category, transType }) => ({
    //     catType: transType,
    //     category,
    //   })
);
